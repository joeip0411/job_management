[project]
name = "linkedin-notion-pipeline"
version = "0.1.0"
description = "Scrape LinkedIn job postings and insert into a Notion database"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "pyyaml>=6.0",
    "google-genai>=1.0.0",
    "weasyprint>=63.0",
]

[dependency-groups]
dev = []
